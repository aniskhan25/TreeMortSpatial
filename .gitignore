# macOS
.DS_Store

# Large data artifacts (regenerate via the pipeline; too large for git/GitHub)
data/*.gpkg
data/*.csv

# Python
__pycache__/
*.pyc
.venv/

# LaTeX build intermediates (main.pdf is kept as the deliverable)
report/*.aux
report/*.log
report/*.out
report/*.blg
report/*.bbl
report/*.synctex.gz

# Regenerable re-cluster caches from the gate scripts
analysis/out/clusters_eps*.csv

# host rasters (MS-NFI etc.)
data/*.tif
data/*.tiff

# exploratory/superseded analysis outputs
analysis/out/host_pcf.json
analysis/out/host_pcf_pooled.json
analysis/out/permtest_null.npy

# R bridge intermediates (regenerable via export_blocks_for_R.py)
analysis/out/rblocks/
